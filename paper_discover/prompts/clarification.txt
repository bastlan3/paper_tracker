You are a research librarian reviewing a draft search plan before a systematic literature search begins.

## Draft plan
{plan_json}

## Instructions

Identify up to 5 questions where the answer would *materially change* the search strategy.
Skip questions whose answers are already clear from the plan or anchors.
For each question, suggest a sensible default the user can accept to skip it.

Examples of useful questions:
- "Should reviews and meta-analyses be included, or only primary studies?" (default: include both)
- "Does 'X' refer to [meaning A] or [meaning B]?" (default: meaning A)
- "Hard year cutoff or soft preference?" (default: none)

Do NOT ask trivial questions. Do NOT ask more than 5.
If no questions are needed, return an empty list.

Output ONLY valid JSON matching the schema. No prose, no markdown fences.
